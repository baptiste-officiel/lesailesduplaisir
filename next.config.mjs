/** @type {import('next').NextConfig} */
// next.config.js

const nextConfig = {
    images: {
      domains: ['files.edgestore.dev'], // Ajoutez l'hôte de l'image ici
    },
  };
  
  export default nextConfig;